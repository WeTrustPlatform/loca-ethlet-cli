{
  "$id": "locaEthletInit",
  "description": "Schema for initialize LocaEthlet instance",
  "type": "object",
  "required": ["rpc", "walletProvider"],
  "properties": {
    "walletProvider": {
      "type": "object",
      "description": "An implementation WalletProvider. Can be found in ./lib/wallet-provider"
    },
    "rpc": {
      "type": "string",
      "format": "uri",
      "description": "URL of an Ethereum node's RPC server"
    }
  }
}
